# identity-front

Authenticates the account and verifies platform attestation. **Never sees a vote.**

First component of the trust path (doc 21). Not yet implemented — the trust
path is built at step 14, after the prototype gate.
