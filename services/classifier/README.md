# classifier

Python. Teen-language NLP, separate from anomaly detection — a cruel caption
is not statistically anomalous.

**Scope depends on AMD 1.** If captions are assembled from a pre-moderated
bank, this collapses to a one-time review of a finite line bank.
