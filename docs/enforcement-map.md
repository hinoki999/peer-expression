# Enforcement map

**Generated by `tools/check-invariants.mjs`. Do not edit by hand.**

Every guarantee, the mechanism that holds it, who owns that mechanism,
what CI actually asserts about this repo, and what is left over.

Per doc 32: a CI check is not an enforcement point. An invariant can
carry a check and still be unenforced — the check supports the guarantee,
it does not deliver it.

`39` specified · `16` enforcement points built · `22` CI checks · `10` end-to-end validated

## client-affordance

### I6 — **NOT BUILT**

> Local history is encrypted at rest

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — the key lifecycle and backup exclusion are built (I6b); the cipher is not. expo-sqlite cannot encrypt, so this needs the op-sqlite/SQLCipher swap the schematic already anticipates, plus reading the file off a device — which no CI job can do. |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — _nothing_
- **residual** — the class is imprecise — encryption constrains the dishonest path too, and doc 03 has no device-control class. An adult with the passcode can still open the app (doc 14 s9), which is a limit of the architecture rather than a gap in it.
- **source** — doc 13 v2 s6

### I16b — in place

> A conforming client emits at most one aggregate contribution per card, however often the answer is revised

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — the outbox enforces it three ways — an enqueue guard, a unique partial index on queued votes, and a vote_sent marker that outlives the drain
- **validated by** — tools/test-gate.mjs drops the unique index and requires the gate to fail
- **residual** — ACCEPTED RESIDUAL (doc 32 C2). Counters accept any submission bearing a valid single-use assertion. Server-side dedup would require exactly the (account, card) ledger the architecture refuses, so a modified client can double-count and nothing server-side will stop it.
- **source** — doc 16 F1, doc 26 S1, doc 32 C2

## server-validation

### I3 — **NOT BUILT**

> The aggregation queue is never durable

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no queue service exists yet — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 15 AMD 7

### I4 — **NOT BUILT**

> Arrival order does not survive the drain

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no queue service exists yet — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 15 AMD 7

### I7 — **NOT BUILT**

> No account receives authored content or statistics computed from another band

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — the vote gateway does not exist — step 14 |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — no read operation in the contract accepts a client-supplied scope
- **residual** — the contract check constrains the declared surface only. The guarantee is a runtime property of the vote gateway, which must derive scope from the signed assertion and reject anything else.
- **source** — doc 16 F6, doc 25 F2, doc 32 C3

### I8 — **NOT BUILT**

> No observable result transition attributable to one participant

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — runtime property of the mill — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 15 Part C

### I9 — **NOT BUILT**

> No publication below MIN_CELL_PUBLIC

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — needs the publication path — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 15 Part C

### I10 — **NOT BUILT**

> Publication gates on batch contents, not schedule

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — needs the publication path — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 15 AMD 5

### I11 — **NOT BUILT**

> A vote never deterministically triggers a visible update

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — needs the publication path — step 14 |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — VoteAccepted declares no statistic field
- **residual** — the contract cannot stop a publication path from updating on receipt; that is the mill’s behaviour
- **source** — doc 15 Part E

### I12 — **NOT BUILT**

> The user's own reveal shows the last published snapshot

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no server ships responses yet — lands with the binding |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — no pending/counted distinction is expressible in the contract types
- **residual** — a field absent from a type is not a field absent from a JSON response (doc 32); the server must not ship one
- **source** — doc 15 AMD 8, doc 32

### I13 — **NOT BUILT**

> Display normalized; no sample size, numerator, decimals, timestamps

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no server ships responses yet — lands with the binding |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — contract types expose no sample size, numerator or decimal precision
- **residual** — covers contract types, not client-rendered strings (doc 32). If the server ships true numbers and the client rounds, a modified client reads the exact figure.
- **source** — doc 15 Part C, doc 32

### I14 — **NOT BUILT**

> Cohort fallback is sticky and never oscillates

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — needs the publication path — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 15 AMD 4

### I18 — **NOT BUILT**

> Authored text never joined to counters

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no query layer exists yet — lands with the binding |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 13 v2 s7

### I19 — **NOT BUILT**

> Every read of authored text and every trust-path access is logged

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no moderation tool or trust path yet — steps 13, 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 16 F8

### I20 — **NOT BUILT**

> No model scores a named user, except integrity — and that exception is fenced

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no prediction model in the schema yet — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 16 F7

### I23 — **NOT BUILT**

> No account-level score outlives the data that produced it

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no integrity store yet — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 18 O2

### I24 — **NOT BUILT**

> A creator sees only the milestone, never the exact usage count

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no server ships responses yet — lands with the binding |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — UsageMilestone exposes no count, and no per-format authenticated lookup exists in the contract
- **residual** — the endpoint must return a bucket; no integer may exist in any response (doc 19 R3, doc 32)
- **source** — doc 19 R3, doc 25 F3, doc 32

### I25 — **NOT BUILT**

> Appeals and support accept no free text

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no appeal or support types yet — step 12 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — T&S operations (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 17

### I32 — **NOT BUILT**

> No TrustAssertion is issued for an age range below the minimum permitted cohort

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — the Integrity Authority does not exist. A client-side gate would be bypassable, so there is nothing honest to build here yet \u2014 OWN 1. |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned \u2014 backend (OWN 1)
- **CI asserts** — _nothing_
- **residual** — the sharp edge is the regulated-region case: a statutory range straddling the floor, such as 12\u201314, MUST NOT be mapped upward into 13\u201315. Refusal is the default and permission is the written exception. Under-13 is neither unbanded nor an invalid signature \u2014 it is a valid platform signal we deliberately refuse, which is why I22 does not cover it. Refusal happens before issuance, never at the gateway.
- **source** — doc 03 Q4/I32, doc 33

## signed-assertion

### I28 — **NOT BUILT**

> No two vote submissions share a trust-assertion key binding

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — needs the issuer — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 21 T1

### I30 — **NOT BUILT**

> The cohort band is signed by our Integrity Authority, never accepted from the client

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — the gateway’s assertion verification does not exist \u2014 doc 32 C3 |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — no contract operation accepts a client-supplied band
- **residual** — holds only once the vote gateway verifies the assertion signature AND rejects submissions whose band is absent, unsigned, or disagrees with the signed one. That step is specified nowhere (doc 32 C3). The platform does not sign the age response; we do (doc 31 s3).
- **source** — doc 21 T3, doc 31, doc 32 C3

## ci-assertion

### I1 — in place

> Every card's subject is SELF, SYMBOL, HYPOTHETICAL or AGGREGATE

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — CARD_SUBJECTS is a closed union of four; the taxonomy gate rejects a seed card with any other subject
- **residual** — no server-side submission schema validates subject at runtime; the gate has no cards to walk yet
- **source** — doc 04

### I2 — in place

> The vote store holds counters, not rows

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — schema definitions in this repo declare no per-vote row, timestamp or account key
- **residual** — the bound database is unbuilt; re-run against the real schema at binding
- **source** — doc 13 v2

### I5 — in place

> Personal history never leaves the device readable

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — no contract operation declares a parameter or return carrying answer history or a flirtprint
- **residual** — a field absent from a type is not a field absent from a runtime response; the binding must validate
- **source** — doc 13 v2

### I6b — in place

> The database key is held by the platform keystore and excluded from cloud backup

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — the keystore adapter passes WHEN_UNLOCKED_THIS_DEVICE_ONLY on every call and no other accessibility value, and app.config.ts sets android.allowBackup false
- **validated by** — tools/test-gate.mjs loosens the keychain accessibility, and separately removes android.allowBackup, and requires the gate to fail on each
- **residual** — asserts the configuration, not the platform behaviour. That iOS honours device-only for Keychain items, and that Android excludes the app from Google Backup, are Apple and Google guarantees — verified by restoring a backup onto a second device, a human process nobody has run. allowBackup is app-wide, so an unrelated config change can silently drop it; this check is what makes that loud.
- **source** — doc 13 v2 s6, doc 14 s9

### I15 — in place

> Cohorts predetermined; no arbitrary slicing

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — PublicationScope is a closed union of the known bands plus the one global scope
- **validated by** — tools/test-gate.mjs widens PublicationScope to string and requires the gate to fail
- **residual** — a server could still compute an unlisted slice internally; the union constrains what our code can name
- **source** — doc 15 Part C

### I16 — in place

> Local history is append-only; a revision inserts and never overwrites

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — the answers module exports no update path and no UPDATE against answer_local
- **residual** — a modified client can write to the SQLite file directly; this binds our own code, not the device
- **source** — doc 16 F1

### I17 — in place

> There is no channel between two users

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — no contract operation accepts a recipient
- **residual** — absence of a feature rather than a control; any new operation re-opens it (doc 14 s2)
- **source** — doc 14 s2

### I21 — **NOT BUILT**

> No question can require disclosure of harm

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — the taxonomy gate has no cards to walk — step 10 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — card library (doc 08)
- **CI asserts** — _nothing_
- **source** — doc 15 Part A

### I22 — in place

> Privacy parameters have hard floors configuration cannot cross

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — MIN_CELL_PUBLIC and MIN_BATCH_DELTA are compared against their floors in this repo
- **validated by** — tools/test-gate.mjs sets MIN_CELL_PUBLIC below its floor and requires the gate to fail
- **residual** — a deployed service could read a threshold from configuration the gate never sees
- **source** — doc 16 F4

### I26 — in place

> A cross-band statistic is never presented as peer data

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — the global scope label differs from every band label in SCOPE_LABEL
- **residual** — client copy elsewhere could still imply peer data; the check covers the label table only
- **source** — doc 18 O1

### I29 — **NOT BUILT**

> The Integrity Authority’s own store holds no account-to-key-fingerprint association

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — needs the issuer — step 14 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — unowned — backend (doc 08)
- **CI asserts** — _nothing_
- **residual** — SPLIT PER DOC 32. The original wording was a universal negative over logs, backups, traces and error reports, which no CI assertion can cover. CI can assert the absence of the column; everything else is a named periodic review with an owner. App Attest and Play Integrity keys are a different class and ARE durably retained — they never cross the gateway (doc 31 s5).
- **source** — doc 21 T2, doc 31 s5, doc 32

### CL5 — in place

> Every emoji sequence used by a card exists in the approved manifest

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie (the gate) \u2014 manifest contents unowned with the card library (OWN 2)
- **CI asserts** — every emoji sequence in a seed card body or option, and in every registered card source, is present in SUPPORTED_EMOJI_SEQUENCES
- **validated by** — tools/test-gate.mjs puts a Unicode 14 sequence into a card source and requires the gate to fail
- **residual** — the manifest is provisional until a card library exists. A sequence can render on the two devices step 4 tested and still fail on an OEM font neither covered (doc 06 v2 s3). The gate constrains content; it does not verify rendering.
- **source** — doc 33 CL5, doc 06 v2 s6, doc 29

### CL3 — in place

> Every card declares its cohort bands explicitly \u2014 no implicit or default all-ages

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — Card.cohortBands is non-optional in the shared model, and the content gate rejects a card whose bands are absent or empty
- **validated by** — tools/test-gate.mjs registers a card with empty cohortBands and requires the gate to fail
- **residual** — declaring a band is not the same as being appropriate for it \u2014 that is CL1b against authored tags, and CL1c, which no check reaches (doc 33).
- **source** — doc 33 CL3

### CL6 — in place

> Every card that can reach a user lives in a source the content gate scans

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — CARD_LIBRARY_SOURCES is iterated by the gate, and a registered source that is missing or unreadable fails the build
- **validated by** — tools/test-gate.mjs removes a registered source and requires the gate to fail rather than quietly scanning less
- **residual** — NOT assertable: that the registry is complete. Nothing can prove a fourth card universe does not exist somewhere unregistered. Doc 33 names the destination \u2014 one authoritative card schema in packages/shared that both seeds and mocks instantiate, so coverage becomes a property of the type rather than a list to maintain.
- **source** — doc 33 CL6

### CL1a — in place

> The band rule sets form a chain — every rule a looser band requires, a stricter band also requires

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — BAND_SAFETY_RULES is walked strictest-first and any rule required by a looser band but not a stricter one fails the build
- **validated by** — tools/test-gate.mjs breaks the chain and requires the gate to fail
- **residual** — true by construction — the sets are derived unions (doc 33 s2.4), so this is a regression test against someone later flattening them into three literals, not the thing holding the property up.
- **source** — doc 33 CL1a

### CL1b — in place

> A card clears every safety rule required by each band it declares

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie
- **CI asserts** — for every declared band, BAND_SAFETY_RULES[band] minus card.restrictionsCleared must be empty, over review-gated sources
- **validated by** — tools/test-gate.mjs seeds a card declaring a band whose rules it does not clear and requires the gate to fail
- **residual** — THE WHOLE GUARANTEE REDUCES TO CL1c, which is Atlas’s (doc 33). This compares tags; nothing here can tell whether a card tagged as clearing R_substance actually avoids substance references. The comparison is deliberately asked as what the BAND requires \u2014 the reverse direction passes trivially for a card with no tags, since the empty set is a subset of everything.
- **source** — doc 33 CL1b

### CL7 — in place

> Every card in the authoritative library carries an APPROVE review record made against the current rule version

| | |
|---|---|
| specified | yes |
| enforcement point built | yes |
| CI / review check exists | yes |
| end-to-end validated | yes |

- **owner** — Caitie (the check) \u2014 the decision is Atlas (OWN 2)
- **CI asserts** — over review-gated sources: a card with no reviewRecord, a disposition other than APPROVE, a missing reviewer or date, or a rulesVersion other than the current one fails the build
- **validated by** — tools/test-gate.mjs seeds a card with no record, and separately one with a stale rulesVersion, and requires the gate to fail on each
- **residual** — the gate cannot decide whether a card is safe; it refuses a card nobody decided about. Development fixtures are not review-gated \u2014 a scope call stated in CARD_LIBRARY_SOURCES, and the residual there is that a team build does put unreviewed mock cards in front of a person.
- **source** — doc 33 CL7

## human-process

### I27 — **NOT BUILT**

> Formative test responses never become production statistics

| | |
|---|---|
| specified | yes |
| enforcement point built | **no** — no prototype build target yet — step 11 |
| CI / review check exists | **no** |
| end-to-end validated | **no** |

- **owner** — Caitie
- **CI asserts** — _nothing_
- **source** — doc 20
