# moderation

Internal reviewer tool. One item at a time through a purpose-built queue,
every view logged (I19). Escalation is deliberately absent — doc 15 resolves
D7 by removing disclosure capability, not by building a pipeline.
