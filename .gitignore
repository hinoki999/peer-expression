# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.turbo/
*.tsbuildinfo

# native
ios/
android/
.expo/

# secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.keystore
*.p8
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist

# research — no third-party corpus is ever stored, here or anywhere
research/corpus/

# local
*.log
.DS_Store
coverage/

Claude outputs/

# Evidence of a self-test run. Never committed — a committed one would be
# a claim about a run that may not have happened on this tree.
.gate/
